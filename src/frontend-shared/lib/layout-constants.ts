/** Centralized layout constants shared across grid and editor components. */

/** Grid header height: 32px content + 2px border */
export const HEADER_HEIGHT = 34;

/** Height of a single data grid row in pixels */
export const ROW_HEIGHT = 32;

/** Default width for grid columns in pixels */
export const DEFAULT_COLUMN_WIDTH = 150;

/** Minimum width for grid columns during resize */
export const MIN_COLUMN_WIDTH = 50;

/** Minimum height for the SQL editor pane in pixels */
export const MIN_EDITOR_HEIGHT = 60;
